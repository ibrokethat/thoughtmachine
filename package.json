{
  "name": "thoughtmachine",
  "description": "ThoughtMachine wants to move you!",
  "version": "0.0.0",
  "author": "Simon Jefford <si@ibrokethat.com>",
  "dependencies": {
    "ampersand-collection": "^1.3.16",
    "ampersand-router": "^1.0.5",
    "ampersand-state": "^4.3.12",
    "ampersand-subcollection": "^1.4.5",
    "ampersand-view": "^7.1.4",
    "config": "0.4.x",
    "debounce": "^1.0.0",
    "js-yaml": "3.2.x",
    "koa": "x",
    "koa-static": "x",
    "underscore": "^1.6.0"
  },
  "devDependencies": {
    "assert": "*",
    "chai": "*",
    "mocha": "*",
    "rewire": "2.0.x",
    "should": "*",
    "sinon": "*",
    "sinon-chai": "*",
    "browserify": "5.10.1",
    "domthing": "^0.3.1",
    "domthingify": "^0.2.0",
    "watchify": "^2.0.0"
  },
  "scripts": {
    "test": "node ./node_modules/mocha/bin/_mocha --colors --recursive --ui bdd --reporter spec ./test --NODE_ENV=test",
    "build": "browserify js/app.js -t domthingify -o build/app.js",
    "start": "watchify js/app.js -t domthingify -o build/app.js"
  }
}
